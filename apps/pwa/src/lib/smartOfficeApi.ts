import { supabase } from '@/lib/supabase'
const GAS_URL=process.env.NEXT_PUBLIC_GAS_WEB_APP_URL||'https://script.google.com/macros/s/AKfycbzzK75gxawaylaUZpoC1zp_hq5ktznlN7scIl24HkdEgR2l3cVmpUSLck0potcMZZtw/exec'
type Performer={name?:string|null,email:string}
export async function smartOfficePost(action:string,payload:Record<string,unknown>,performed_by:Performer){const{data:{session}}=await supabase.auth.getSession();if(!session?.access_token)throw new Error('Session tidak tersedia. Login ulang.');const res=await fetch(GAS_URL,{method:'POST',headers:{'Content-Type':'text/plain'},body:JSON.stringify({...payload,action,access_token:session.access_token})});const data=await res.json();if(data?.success===false||data?.ok===false)throw new Error(data?.message||data?.error||'Smart Office request gagal');return data}
export async function createEmployeeDocumentRequest(args:{documentType:'SIZ'|'SKT'|'ST'|'PI',employeeId:string,subject:string,extra?:Record<string,unknown>,performedBy:Performer}){return smartOfficePost('so_create_draft',{documentType:args.documentType,company_code:'RIFIM',subject:args.subject,request_source:'RAOS',extra:{...(args.extra||{}),employee_id:args.employeeId}},args.performedBy)}
export async function submitEmployeeDocumentRequest(documentId:string,performedBy:Performer){return smartOfficePost('so_submit',{documentId,request_source:'RAOS'},performedBy)}
