import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.110.5'
const SUPABASE_URL=Deno.env.get('SUPABASE_URL')||''
function resolveAdminKey(){const single=Deno.env.get('SUPABASE_SECRET_KEY');if(single)return single;return Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||''}
function allowed(origin:string|null){if(!origin)return true;try{const u=new URL(origin);return u.hostname==='raos-menala.vercel.app'||u.hostname==='rifim-os.vercel.app'||u.hostname.endsWith('.vercel.app')||u.hostname==='localhost'||u.hostname==='127.0.0.1'}catch{return false}}
function cors(origin:string|null){return{'Access-Control-Allow-Origin':origin&&allowed(origin)?origin:'https://raos-menala.vercel.app','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Cache-Control':'no-store','Vary':'Origin'}}
Deno.serve(async req=>{const origin=req.headers.get('origin'),headers=cors(origin);if(req.method==='OPTIONS')return new Response('ok',{headers});if(req.method!=='POST')return Response.json({ok:false,reason:'method_not_allowed'},{status:405,headers});if(!allowed(origin))return Response.json({ok:false,reason:'origin_not_allowed'},{status:403,headers});const key=resolveAdminKey();if(!SUPABASE_URL||!key)return Response.json({ok:false,reason:'server_not_configured'},{status:503,headers});try{const body=await req.json(),login_id=String(body?.login_id||'').trim().slice(0,160),raos_pin=String(body?.raos_pin||'').trim().slice(0,64);if(!login_id||!raos_pin)return Response.json({ok:false,reason:'invalid_input'},{status:400,headers});const admin=createClient(SUPABASE_URL,key,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});let verified:any=null;const safe=await admin.rpc('raos_verify_login_secret',{p_login_id:login_id,p_raos_pin:raos_pin});if(!safe.error&&safe.data?.ok&&safe.data?.email)verified=safe.data;else{const legacy=await admin.rpc('raos_verify_and_bridge',{p_login_id:login_id,p_raos_pin:raos_pin});if(!legacy.error&&legacy.data?.ok&&legacy.data?.email)verified=legacy.data;else return Response.json({ok:false,reason:legacy.data?.reason||'invalid_credentials',message:legacy.data?.message||'ID/email atau PIN RAOS salah.'},{status:401,headers})}const{data:link,error:linkError}=await admin.auth.admin.generateLink({type:'magiclink',email:verified.email});const tokenHash=link?.properties?.hashed_token;if(linkError||!tokenHash)return Response.json({ok:false,reason:'session_exchange_failed'},{status:500,headers});return Response.json({ok:true,token_hash:tokenHash,verification_type:'email',role:verified.role,user_id:verified.user_id},{headers})}catch{return Response.json({ok:false,reason:'bad_request'},{status:400,headers})}})
